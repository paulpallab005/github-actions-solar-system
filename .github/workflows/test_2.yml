name: test_2

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Print environment name
        run: echo "Environment Name: ${{ github.event.deployment.environment }}"
